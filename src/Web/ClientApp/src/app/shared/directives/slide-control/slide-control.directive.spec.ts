import { SlideControlDirective } from './slide-control.directive';

describe('SlideControlDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideControlDirective();
    expect(directive).toBeTruthy();
  });
});
