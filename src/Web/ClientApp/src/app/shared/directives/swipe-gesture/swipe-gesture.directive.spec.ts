import { SwipeGestureDirective } from './swipe-gesture.directive';

describe('SwipeGestureDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeGestureDirective();
    expect(directive).toBeTruthy();
  });
});
